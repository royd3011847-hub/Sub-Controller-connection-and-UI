# Research
    requests library
        post request
        send a json (all you need to give the function is a python dict)
    some way to receive controller input
        inputs library, hidapi library, py object library maybe
        this MIGHT need a separate thread for receiving input
    IF NECESSARY threading library

# Actually do
    Fix the UI
        get rid of connect/disconnect
        move kill
        add a terminal to left side
    Fix connect
        use requests instead of socket

